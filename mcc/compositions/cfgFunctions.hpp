class compositions {
	#ifdef MCCMODE
	file = "\mcc_sandbox_mod\mcc\compositions\fnc";
	#else
	file = "mcc\compositions\fnc";
	#endif

	class compositionsGrab {description = "Grab a composition that can be saved in cfgGroups to load via Zeus 3den or MCC";};
	class compositionsPlace {description = "Place a composition as defined by config entry";};
};